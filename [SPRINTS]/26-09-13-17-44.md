# Спринт 26-09-13 17:44 МСК

1. [~] Динамический файловый каталог серверных шаблонов.
   - НАЧАЛ: отделён файловый сайт от интерфейса редактора и пользовательских каталогов.
   - СДЕЛАЛ: добавлен закрытый PHP-каталог, который сканирует реальные PNG/JSON-файлы; `manifest.json` отвечает только за подписи и порядок; добавлены три подтверждённых PNG-проекта.
   - ПРОВЕРИЛ: состав файлов, JSON, хеши копий, правила допустимых расширений и отсутствие CORS-заголовков. Локального PHP-интерпретатора нет; runtime-проверка остаётся после публикации на сервер.
   - ЗАКОНЧИЛ: файловая часть готова к отдельному commit/push; пользовательская проверка endpoint ещё не выполнена.

2. [~] Версии оригиналов для общего сервиса превью.
   - НАЧАЛ: пользователь загрузил три PNG под фактическими именами `bzn-project-with-images*.png`; полный файл не должен загружаться карточкой.
   - СДЕЛАЛ: metadata сопоставляет фактические имена с тремя подписями; прежние Git-имена остаются совместимыми файлами, но исключены из каталога как дубли. Каждая запись получает revision из имени, времени и размера оригинала.
   - ПРОВЕРИЛ: три загруженных URL отвечают HTTP 200; JSON и diff-check проходят. Локального PHP-интерпретатора нет; `manifest.php` на сервере пока отвечает 404 до pull.
   - ЗАКОНЧИЛ: изменение готово к отдельному commit/push; пользовательская проверка списка после pull остаётся.

3. [~] Развёртывание каталога без PNG в Git.
   - НАЧАЛ: проверено отличие файлового хранилища от Git-checkout и причина неработающего `manifest.php`.
   - СДЕЛАЛ: PNG объявлены серверными runtime-файлами и исключены из Git; статический `manifest.json` получил версии трёх фактических PNG по SHA-256.
   - ПРОВЕРИЛ: локальный `main` до правки совпадал с `origin/main`; серверные PNG побайтно совпали с прежними Git-копиями. Веб-сервер `files.bsns.ru` отдаёт PHP как исходный файл, поэтому текущий шлюз должен читать JSON.
   - ПРОВЕРИЛ: все три локальные PNG физически сохранены и совпадают с серверными по SHA-256; Git их игнорирует. `manifest.json` прошёл три цикла разбора; `git diff --check` не нашёл ошибок.
   - ЗАКОНЧИЛ: PHP и manifest остаются в Git, только PNG сняты с отслеживания; ожидаются отдельный commit/push и серверное подключение.
