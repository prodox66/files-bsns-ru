# Спринт 20.09.2026 04:44 МСК

1. [~] Опубликовать runtime manifest для шаблонов на статическом `files.bsns.ru`.
   - Manifest содержит только ID, имя, MIME, размер и дату ресурса.
   - Сборщик находится в PHP-репозитории `library-api-bsns-ru`; сам manifest — изменяемые runtime-данные и игнорируется Git.
   - `.gitignore` и описание runtime-манифеста добавлены коммитом `ac899ca` и развёрнуты ff-only.
   - Production manifest собран по 6 файлам и отдаётся HTTP 200; все demo PNG приведены к читаемым nginx правам `644`.
   - Манифест и прежние runtime-файлы игнорируются; server checkout остаётся чистым.
   - Нужно явное подтверждение пользователя.
