# Спринт 20.09.2026 04:44 МСК

1. [ ] Опубликовать runtime manifest для шаблонов на статическом `files.bsns.ru`.
   - Manifest содержит только ID, имя, MIME, размер и дату ресурса.
   - Сборщик находится в PHP-репозитории `library-api-bsns-ru`; сам manifest — изменяемые runtime-данные и игнорируется Git.
   - Нужны server deploy, сборка по всем файлам и live HTTP-проверка.
