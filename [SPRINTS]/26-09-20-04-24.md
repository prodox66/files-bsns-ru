# Спринт 20.09.2026 04:24 МСК

1. [-] Универсальный межсерверный HTTP API ресурсов на `files.bsns.ru`.
   - Вход: логический тип ресурса, scope, непрозрачный owner, действие и имя файла.
   - Выход: нормализованный JSON-список либо байты выбранного файла.
   - Физические пути, структура хранилища и service key не раскрываются браузеру.
   - Первая реализация обслуживает публичные и пользовательские PNG-шаблоны; новые хранилища добавляются конфигурацией/адаптером.
   - Добавленные config, endpoint, classes и tests прошли PHP lint и focused contract 3/3.
   - Живой HTTP-запрос доказал, что домен статический: `.php` вернулся как исходный текст с HTTP 200 и не выполнил service-key проверку.
   - Собственные PHP-файлы этой попытки точечно удалены; runtime `templates/` и `users/` не менялись.
   - Следующая отдельная реализация должна жить на PHP-домене `images.bsns.ru`, а этот домен остаётся сменным статическим storage/CDN.
